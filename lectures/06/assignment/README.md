# Assignment Ideas

Instructions in README.md, one scaffold notebook per problem. Start each notebook with %pip install -r requirements.txt --quiete, and emnist download (if not exists) or RNN data generator script.

1. Basic NN & evaluation on EMNIST: classify letter vs digit.
2. XGBoost vs CNN (from scratch) on EMNIST, letters a >> g (or all characters if up to it).
3. RNN from scratch on simulated data (generated by example script)...?

NOTE: Make autogradable through saved outputs in results/ and possibly models/

<!--- 

Chicken scratch below here 

## 🦾Exercise: EMNIST Classification

### Task: Classify handwritten characters using the EMNIST dataset

#### Step 1: Choose a Model

Select a suitable neural network architecture for this task. Consider using Convolutional Neural Networks (CNNs) or other appropriate models.

#### Step 2: Train the Model

Implement your chosen model using Keras or PyTorch. Train the model on the EMNIST dataset.

#### Step 3: Evaluate the Model

Evaluate your model's performance on the test set. Analyze the confusion matrix to identify where the model performs well and where it struggles.

#### Step 4: Investigate Subsets

Examine the model's performance on different subsets of the data. For example, evaluate its performance on specific classes or groups of classes.

#### Step 5: Improve Performance

Brainstorm ways to improve the model's performance. This could involve tuning hyperparameters, using data augmentation, or trying different architectures.

----


## 🦾Exercise

### 1. Classify all symbols

#### Choose a model

Your choice of model! Choose wisely…

#### Train away

Is do you need to tune any parameters? Is the model expecting data in a different format?

#### Evaluate the model

Evaluate the models on the test set, analyze the confusion matrix to see where the model performs well and where it struggles.

#### Investigate subsets

On which classes does the model perform well? Poorly? Evaluate again, excluding easily confused symbols (such as 'O' and '0').

#### Improve performance

Brainstorm for improving the performance. This could include trying different architectures, adding more layers, changing the loss function, or using data augmentation techniques.

--->