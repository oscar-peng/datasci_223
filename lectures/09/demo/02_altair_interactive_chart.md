# Demo 2: Interactive Altair Chart

This demo focuses on creating an interactive chart with Altair, including a selection mechanism.

**Task:** Using a dataset from PhysioNet (or a provided sample), create an interactive Altair chart that includes a selection mechanism (such as an interval brush for selecting a range of data, or a dropdown menu for filtering by a category). The chart should then be saved in JSON format.